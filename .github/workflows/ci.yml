name: Test and lint

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.9", "3.11"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest>=8.0 pytest-mock>=3.12 pytest-cov>=5.0 ruff>=0.6 aiohttp>=3.9 numpy

      - name: Ruff
        run: python -m ruff check tests/ h3_models.py h3_pricing.py h3_payloads.py h3_backends.py h3_video_node.py h3_integration.py digit_video_common.py muapi_client.py projekts_utils.py image_saver_node.py image_loader_node.py video_saver_node.py

      - name: Pytest
        run: python -m pytest --cov --cov-report=term-missing
