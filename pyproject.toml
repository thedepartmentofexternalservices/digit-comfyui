[project]
name = "comfyui-digit"
version = "4.0.1"
description = "DIGIT — Deep Infrastructure for Generative Image Training. ComfyUI nodes for Gemini generation, Veo + Seedance 2.0 + MiniMax H3 video, LoRA training, dataset management, and auto-captioning."
license = { text = "MIT" }
requires-python = ">=3.9"
dependencies = [
    "requests",
    "google-auth",
    "google-genai",
    "google-cloud-storage",
    "piexif",
    "Pillow",
    "opencv-python",
    "pyyaml",
    "fal-client",
    "replicate",
    "soundfile",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-mock>=3.12",
    "pytest-cov>=5.0",
    "ruff>=0.6",
    "aiohttp>=3.9",
    "numpy",
]
training = [
    "torch>=2.1.0",
    "diffusers>=0.30.0",
    "transformers>=4.40.0",
    "peft>=0.12.0",
    "accelerate>=0.30.0",
    "bitsandbytes>=0.43.0",
    "tensorboard",
    "wandb",
    "safetensors",
]

[project.urls]
Repository = "https://github.com/thedepartmentofexternalservices/comfyui-digit"

[tool.comfy]
PublisherId = "digit"
DisplayName = "DIGIT Nodes"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
consider_namespace_packages = false
markers = [
    "integration: requires ComfyUI install, live APIs, or pipeline mounts",
]
addopts = "-m 'not integration' -p tests.pytest_plugin"
norecursedirs = ["scripts", "web", "workflows", "configs", ".git", ".pytest_cache"]

[tool.ruff]
target-version = "py39"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
"*_node.py" = ["E402"]
"__init__.py" = ["E402", "F401"]
"legacy_aliases.py" = ["E402", "F401"]

[tool.coverage.run]
include = [
    "projekts_utils.py",
    "image_saver_node.py",
    "image_loader_node.py",
    "video_saver_node.py",
    "uber_saver_node.py",
]
omit = ["tests/*"]

[tool.coverage.report]
fail_under = 60
show_missing = true
